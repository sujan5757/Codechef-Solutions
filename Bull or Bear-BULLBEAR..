#include <stdio.h>

int main(void) {
	int i;
	scanf("%d",&i);
	while(i--)
	{
	    int x,y;
	    scanf("%d%d",&x,&y);
	    if(x==y)
	    printf("NEUTRAL\n");
	    else if(x>y)
	    printf("LOSS\n");
	    else if(x<y)
	    printf("PROFIT\n");
	    else{
	        break;
	    }
	}
	return 0;
}
