#include <stdio.h>

int main(void) { 
    int t; 
    scanf("%d",&t); 
    while(t--){ 
        int a,b; 
        scanf("%d%d",&a,&b); 
        if(b>a){ 
            printf("\n0"); } 
            else
        printf("\n%d",abs(a-b)); 
    }
	// your code goes here
	return 0;
}

